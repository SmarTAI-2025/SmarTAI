# SmarTAI 当前项目记忆与决策

> 最后更新：2026-08-11
> 用途：跨 Codex 任务保存已经确认的产品、架构、部署和运营决定。这里记录“现在相信什么”，不保存密码、API Key、令牌或其他秘密。

## 新任务读取规则

1. 回答任何 SmarTAI 项目问题前，先完整读取本文件。
2. 周进度、负责人、阻塞和发布状态不要从本文件猜测；继续读取：
   - `docs/project_management/负责人项目总控与逐周核查清单.md`
   - `docs/project_management/PROJECT_MANAGEMENT_LEDGER_AI.json`
   - 运行 `python tools/project_management/check_beta_status.py --date YYYY-MM-DD`
3. 用户确认、改变或撤销重要决定后，当次任务必须同步更新本文件，写清日期、状态、来源任务和替代了什么。
4. 新讨论不会自动推翻旧决定。只有用户明确确认，或新的项目文档被指定为来源，才能把决定标成“已替代”。

## 状态含义

- **已确认**：用户已经决定，应作为后续默认前提。
- **正在评估**：正在讨论或验证，不得写成已经拍板。
- **待执行/待验证**：方向已确认，但尚无完成证据。
- **已替代**：不再作为当前方案，只保留原因和历史来源。

## 当前已确认决定

### 云账号与部署

1. **继续申请 AWS，不因 Oracle 或新 App 讨论取消。**
   - 状态：**已确认方向；账号注册/额度领取结果待验证。**
   - 原因：Oracle Free Tier 实际无法成功注册，不能继续作为关键路径；AWS 是可执行的海外部署和 credits 备选。
   - 来源任务：`019fc7b0-c9a9-7b20-8750-75c2911b3b41`，2026-08-06 的 AWS 注册、Free Plan、credits、Lightsail 与腾讯云迁移讨论。
   - 重要纠正：后续不得把该决定概括成“不要申请 AWS”。关于 App/local-first 的新讨论只会改变将来需要购买的 AWS 配置，尚未撤销申请账号的决定。

2. **App 方向确定前，AWS 的既有部署基线是新加坡 Lightsail，而不是复杂的 EC2/RDS 拼装。**
   - 状态：**部署基线已确认；是否实际创建以及最终规格待架构决定。**
   - 原计划：先选保守规格、使用 Free Plan/credits、设置预算和费用告警；需要扩大时再升配，并保持 Docker、PostgreSQL 标准备份和 S3 兼容文件接口，方便以后迁腾讯云。
   - 不提前购买大规格：如果桌面 App 承担本地数据库和批改执行，AWS 可能只剩认证、共享模型代理或同步服务，所需规格会明显降低。

3. **Oracle A1 不再作为当前首选或上线依赖。**
   - 状态：**已替代。**
   - 原因：项目负责人确认 Oracle Free Tier 注册无法成功。
   - 以后即使偶然注册成功，也只能作为可替换测试/备用资源，不能重新变成默认方案，除非用户再次明确决定。

4. **Cloudflare Pages 继续作为 Web 静态前端基线。**
   - 状态：**已确认。**
   - 当前测试入口仍可使用 `smartai-course.pages.dev`；正式域名、邮件和后端地址按发布门禁另行确认。

### 成本与模型

1. **BYOK 仍是默认成本控制方式；共享模型池必须有个人额度、平台总熔断、并发限制和一键停用。**
2. **云服务 credits 是过渡资源，不等于永久免费。** AWS Free Plan、credits 到期和升级 Paid Plan 后的扣费边界必须在创建资源前再次按账号控制台核对。
3. **真实学生数据上线前必须通过** `docs/active_beta_launch/20260803_leader_replan/PRE_PRODUCTION_SECURITY_RELEASE_GATE_CN.md`；购买服务器不等于获准上线。
4. **平台共享 Unlimited-OCR token 延后到上线后优先 02。**
   - 状态：**已确认；受邀测试版生产入口保持关闭。**
   - 上线后优先 01 永远先处理首批教师暴露的安全、数据丢失/错配、费用失控或主流程 P0/P1；这些问题稳定后，才评估打开平台共享 Unlimited-OCR。
   - 上线前可以完成该服务的 adapter、用户自有凭据路径和去标识化内部 smoke，但 production 不展示共享选择器、不配置可被普通用户调用的共享路由，也不把它计入一次性免费试用包。
   - 未来开放前必须完成按请求/页数/费用的持久原子预留与结算、retry 幂等、并发与个人上限、平台总熔断、一键停用和厂商数据条款复核；“Unlimited”不代表用户或平台可以无限消费。
   - 这个决定只延后 Unlimited-OCR 的平台共享开放，不替代或削弱现有平台文字/视觉模型在上线前必须完成的共享池门禁。
   - 来源：2026-08-10 当前 SmarTAI 计划任务中用户明确确认；替代此前把计划 6 作为上线前候选工作的提议，不替代通用免费池计划。

### 教师可诊断的作答识别反馈

1. **教师界面不得只显示笼统的 `failed`、`parse_failed` 或“识别失败”。**
   - 状态：**产品语义已确认；本地集成候选已实现，提交、审核和部署仍待执行/验证。**
   - 每份上传或压缩包成员都要保留独立结果；失败项不能被静默丢弃，重复学生身份不能互相覆盖。
   - 外部 `failed` 只作为统计桶；API 和教师界面必须继续携带安全的具体原因码、失败阶段、任务/来源编号、是否适合重试、易懂说明和下一步建议。
   - `parse_failed` 表示没有得到可用结构的内部终态，实际原因应进一步区分，例如视觉模型不支持 OCR、OCR 返回空文字、模型超时/不可达/限流/鉴权失败、结构化返回无效、文件或压缩包问题。
   - “没有提取到任何作答内容”与“已提取作答但题号全部不属于当前任务”必须分开；后二者都不等于学生答错。
   - 原始供应商报错、堆栈或敏感信息不得直接展示给教师，只投影白名单原因码和安全文案。
   - 来源任务：`019fe5a2-5665-7051-938c-ac8857337c59`，2026-08-09 的作答来源结果与教师反馈根因修复。

### 团队 GitHub 身份映射

1. **批改与作答识别负责人 dsy 的 GitHub 账号是 `desuyagn`。**
   - 状态：**已确认。**
   - 后续 PR 分工、审核请求和项目台账直接使用 `desuyagn`，不再标为候选账号或待确认。
   - 来源：项目负责人于 2026-08-10 在当前 PR 合并清单任务中明确确认。

### AWS Frontier 报名 Demo 边界

1. **宣传页可以使用合成数据、前端动画和预计算 walkthrough；进入 Live Demo 后，API、OCR 和批改必须真实运行。**
   - 状态：**已确认；隔离分支候选已实现，真实 provider E2E、部署和运行证据待完成。**
   - 无需登录的 `/frontier` 必须把动画结果标成 product walkthrough / synthetic content，并保持与真实产品一致的视觉和术语。
   - `/frontier/enter` 从后端取得免密码、无 refresh cookie 的短时随机 Demo 会话，再进入 `/frontier/live`；Live 使用无真实学生信息的合成原始文件，但必须真实创建任务、调用题目识别、视觉 OCR、作答识别和大模型批改，不得把预计算分数注入本次结果。
   - 真实运行失败时先显示安全的真实错误，再由用户主动打开预计算 walkthrough；不得静默降级。
   - 不在网页、URL、仓库或 fixture 中写入账号密码、共享 Token 或 API Key；Gemini Key 只保存在后端。2026-08-11 项目负责人为节约截止前实现成本，明确用后端签发的短时 `frontier_demo` task scope 替代“私下发送每位评审账号密码”。该 scope 只进入 task API，普通 experts/courses/admin 教师面继续拒绝；签发有单进程每日上限和冷却。
   - 这次明确接受的截止前简化边界是：未实现 fixture 上传白名单或完整 demo role，Demo task scope 仍可调用整个 task API；签发计数也不是多 worker 持久额度。部署环境只允许合成数据，这些限制必须作为公开部署风险保留，不得包装成完整生产隔离。
   - 题目审核与作答校对页复用既有 50/50 原文件 Preview UI；Demo 中显示的是 manifest SHA-256 校验后、与实际上传 API 相同的浏览器端 raw fixture 副本。当前后端未持久化原始文件字节，不得宣称这是服务端原件读取。
   - `/frontier`、`/frontier/enter` 和 `/frontier/live` 支持中英文切换；Live 批改设置按启动时界面语言写入 `feedback_language`。
   - `/frontier` 的四阶段宣传 walkthrough 独占整行并固定为 `Source → Recognize → Grade → Analyze`，各阶段分别展示原件输入、原件与结构化识别结果并排复核、rubric/代码测试支持的初评和班级分析；原来的侧栏空间并入价值主张、真实 Demo CTA 与可信边界，不另塞无关功能。
   - 当前后端不提供逐字符 OCR 候选、逐字符置信度或精确区域坐标；宣传页不得展示 `μ/u` 候选选择器、虚构的字符置信度或精确框选映射，只能如实展示“完整原件 + 可编辑结构化文本 + 教师确认”。分析示例只使用当前 Analytics Agent 实际收到的评分数据及其已支持的图表类型，不得在无上下文证据时宣称分析了隐藏测试、原文件格式或 rubric 来源。
   - Live Demo 不得用 fixture 关键词或语义锚点检验真实题目识别结果，也不得用已知 fixture 题干覆盖真实识别文本。页面必须并排展示真实识别题干与预设合成教师 rubric/reference，等待评审显式确认后，才按返回的唯一 Q1–Q4 题号关联；题号缺失或重复时才在写入前停止。这是 Demo 编排边界，不是主体识别算法。
   - 宣传文案动作边界固定为：系统“识别、建议、标记”，教师“复核、修改、决定”。产品定位覆盖复杂计算、推导、证明与编程等广泛理工作答；混合题型只是 Demo fixture 的覆盖设计，不是产品核心定位。
   - 原文件对照之后可用明确标注的合成 walkthrough 展示 `Ask SmarTAI` 自然语言追问和按问题生成的新图表；它与真实 Live API 入口保持视觉一致，但不得冒充本次 Live 运行结果。分析舞台采用明亮、等高的双面板，并以散点图、箱线图和饼图展示不同问题；中英文大标题采用短语级换行，避免拆开词组或产生孤行。
   - Demo 从独立冻结 SHA 部署，不自动吸收 `main` 后续开发改动。
   - 来源：2026-08-10 AWS From Idea to Frontier 报名 Demo 任务中项目负责人明确确认；替代此前“报名 Demo 只做无需登录纯前端 guided demo”的建议。

## 正在评估的产品与架构方向

### Desktop local-first App

状态：**用户明确偏好，正在评估；尚未完成最终架构确认，也未替代当前 Web 测试版。**

当前偏好是让桌面 App 成为教学业务数据的本地主库，包括：

- 题目、学生作答原文件和资料库；
- 每次 OCR/识别后的结构化数据及教师确认版本；
- 批改结果、修改历史、分析和导出；
- 本地任务状态、索引和缓存；
- BYOK，但密钥应进入系统钥匙串或 Tauri Stronghold，不能进入普通 SQLite、日志或默认备份。

建议验证路径是复用现有 React/Vite 页面，以 Tauri 打包，并把现有 Python Agent -> Skill -> Tool 批改引擎作为 sidecar；现有 SQLAlchemy SQLite 轻量模式可以作为本地业务数据库基础，避免重写全部批改逻辑。

如果最终采用这一方向，云端最小控制面仍可能包括用户/密码哈希、邮箱验证、refresh session、邀请码/权益、共享池额度和安全审计。平台共享 API Key 不能打包进 App，使用共享模型时仍需云端代理。

### 尚未拍板的问题

- App 是否成为主产品，Web 是否退为官网、演示、下载和账号管理入口；
- Python 批改引擎是否全部随 App 本地运行，还是仍有短期云端任务；
- 是否需要学生登录提交、教师多设备同步或多人协作；这些能力会重新引入云端业务数据或加密同步层；
- 本地 SQLite/文件的加密、自动备份、恢复、换机和磁盘空间策略；
- AWS 最终只做账号/共享模型控制面，还是仍运行完整后端；在这些问题确认前不购买大规格资源。

## 当前动态进度入口

本文件不复制每周任务完成度，避免产生第二份过期进度表。新任务必须按 `AGENTS.md` 的“Project Management Source of Truth”读取总控、验证机器台账并运行日期核查脚本。当前发布结论只有在这些证据核对后才能回答。

## 已替代或禁止继续沿用的表述

- “Oracle A1 是当前首选/当前暂定上线方案”——已被注册失败事实替代。
- “不要申请 AWS”——不是用户决定；正确表述是继续申请账号，但 App 架构确认前暂缓购买大规格资源。
- “已经决定完整 App 取代 Web”——尚未拍板；目前是强偏好和技术评估方向。
- “所有处理都在本地”——只有本地模型或本地批改引擎且不调用外部模型时才成立；BYOK 直接调用模型厂商仍会把相应数据发送给该厂商。
- “平台共享 Unlimited-OCR 会随受邀测试版上线”——已被 2026-08-10 的范围决定替代；上线前只允许 adapter、用户自有凭据和内部 smoke，平台共享入口排在上线后优先 02。
- “AWS Frontier Demo 必须私下发送每位评审账号密码”——已被 2026-08-11 的免密码短时 Demo task scope 替代；密钥仍只保存在后端，真实 provider E2E 与部署证据仍待完成。

## 更新记录

- **2026-08-11**：项目负责人撤销此前 Demo 编排中的逐题语义锚点护栏，明确该约束既不能提升识别，也不属于主体识别流程；Live Demo 必须保留真实识别题干，先向评审完整展示预设合成教师 rubric/reference 并等待显式确认，再按唯一 Q1–Q4 题号关联，题号缺失或重复时才停止。替代设计文档中“题号和稳定语义锚点都必须匹配”的旧方案。
- **2026-08-11**：项目负责人确认 AWS Frontier 宣传页不得超出当前产品能力：删除逐字符 OCR 候选、字符置信度与精确区域映射暗示；系统文案只使用“识别、建议、标记”，最终由教师复核、修改和决定。分析示例只基于 Analytics Agent 当前可用的评分数据与已支持图表；宣传定位覆盖复杂计算、推导、证明和编程等广泛理工作答，混合题型仅用于 Demo 覆盖。同期确认 Stage 04 改为明亮等高双面板、散点/箱线/饼图，并对中英文大标题做短语级换行。
- **2026-08-11**：项目负责人确认所有公开宣传、Demo 入口、Live Demo 任务名和页面返回链接均使用通用的 SmarTAI 产品品牌，不再显示 AWS、Amazon、亚马逊云科技、From Idea to Frontier 或“报名展示”等活动字样，以便同一页面长期作为通用产品展示。为避免临近截止增加部署风险，内部 `/frontier` 路由、分支、PR 与设计文档文件名暂不重命名；本决定仅替代此前公开页面的活动专属品牌文案，不改变 AWS 申请或部署评估决定。
- **2026-08-11**：项目负责人确认 AWS Frontier 宣传页可把 walkthrough 放大为独占整行的 `Source → Recognize → Grade → Analyze` 四阶段演示；原侧栏空间并入上方价值主张、真实 Demo CTA 与可信边界，并增加双语 `Ask SmarTAI` 合成互动来展示自然语言分析和按问题生成图表。Live API/OCR/批改真实运行边界不变。
- **2026-08-11**：项目负责人确认 AWS Frontier Live Demo 改为点击即取得后端签发的短时免密码 task scope，Gemini Key 只留在后端；为节约截止前实现成本，不做 fixture 白名单或完整 demo role，保留整个 task API 可用、单进程签发计数等已知风险。同期确认复用既有 50/50 Preview UI 显示经 SHA-256 校验的同一浏览器端合成 raw fixture，并为宣传、入口和 Live 页面提供中英文切换；替代此前私下发放每位评审账号密码的运行方案。
- **2026-08-10**：确认 AWS Frontier 报名宣传页可使用明确标注的合成动画和预计算 walkthrough，但登录后的 Live Demo 必须使用合成原始文件真实调用 API、OCR 和批改；失败不得静默切换静态结果，且不得公开账号密码或模型密钥。落实时每位评审使用独立短期账号，不复用普通教师账号凭据；当前没有受限 demo role，环境内只放合成数据。
- **2026-08-10**：项目负责人确认批改与作答识别负责人 dsy 的 GitHub 账号为 `desuyagn`；项目台账和 PR 行动清单不再保留 candidate/待确认表述。
- **2026-08-10**：确认平台共享 Unlimited-OCR token 不进入 2026-08-30 受邀测试版，排为上线后优先 02；上线前仍可准备 adapter、用户自有凭据和内部 smoke，同时保留现有共享模型池全部上线门禁。
- **2026-08-09**：确认教师作答识别反馈必须逐文件、可诊断且给出具体处理建议；区分内部统计状态、外部统计桶和安全原因码，并明确“无作答内容”“题号不匹配”“学生答错”不是同一语义。
- **2026-08-09**：建立跨任务项目记忆；从任务 `019fc7b0-c9a9-7b20-8750-75c2911b3b41` 恢复 AWS 申请/Lightsail 决定；记录 Oracle 注册失败；把 local-first App 标为正在评估而不是已经替代 AWS。
