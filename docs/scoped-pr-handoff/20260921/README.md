# 分功能 PR 交接入口（2026-09-21）

#79 已撤回关闭，旧“只合 #79”指引无效。功能顺序：#80 → #81 → #82 → #83 → #60 → #61 → #62 → #63 → #64 → #65 → #66 → #67；本纯文档 PR 最后。每次轮到某 PR 时，其前序已合入 main，再把当前 base 改为 main，使用 Merge commit。不要直接合到父功能分支。

先读 `02_合并顺序与审核_人读版.md`，转达成员时用 `03_成员通知与后续开发_人读版.md`。`01` 解释最小修复和与 #79 的等价性，`04` 记录继续采用的大题/BYOK/可恢复清理方案；四类均有人读、AI 两版。

feature-manifest.json 是文件/提交/PR 归属；verification-final.json 是最后复核；ci-exact-heads.json 是各功能 PR 当前 CI。原错误评论保留，并以新更正评论覆盖操作指引。#40/#59 已恢复 open 但不重复合并；#58 的重新打开被 GitHub 422 拒绝，#82 单独覆盖必要 PDF 安全修复。

当前没有执行三名独立 subagent；确定性核验和测试不是代理。没有代为 APPROVE 或 merge。真实上线验收不是本次合代码的结论。
