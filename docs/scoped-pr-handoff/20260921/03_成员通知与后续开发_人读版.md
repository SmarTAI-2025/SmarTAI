# 可以直接转发的成员通知（2026-09-21）

## 使用说明

交付时只是提交／推送／PR 整理完成，助手没有合并任何 PR。下面分“合并前可发”和“确实合并后才发”；不要现在把后者写成事实。逐功能顺序见《02_合并顺序与审核》。#79 已关闭，不能再转发“只合 #79”。

## 给 lyj（04A；GitHub lyj00422）——合并前

> 我把 PR 按原功能拆回了，没有改写你的原提交。#59 已恢复打开，但它的原作者历史已包含在 #61，旧迁移编号也由 #61 统一到 0013/0014，因此先不要单独合 #59。#61 保留现有注册、找回密码和前端；这次只补了找回密码在中性 HTTP 响应之后才查账号和发信，避免 SMTP 等待暴露账号存在，并补了回归。不是新增持久邮件队列。请看 #61 的认证、token/session 失效和迁移边界；你方便时可完成本 PR 正常审核，不需要重写代码。若由其他熟悉这块的人先审也可以，不必卡在等你本人。后端依赖升级独立放在 #80，不混进 #61。

合并后可补发：

> #61 已按顺序合入 main（发送前请填实际 merge SHA）。原 #59 内容已承接，不再重复合旧迁移。后续改认证请从最新 main 建分支，并保留“中性响应先完成、全 refresh 撤销、旧 access 失效”回归。真实 Gmail 和 HTTPS origin 仍要在候选上实测。

## 给 #60 原作者——合并前

> #60 已恢复成独立 PR；没有撤销已接受的置信度策略、批改进度和兄弟答案复核状态修复。这次只加了 reporter 实例所有权检查，避免重复领取失败或旧 worker 退出时误删新进度；错误日志保留安全分类、不转储原始异常。与本功能无关的 SmarTAI-shared 文档删改从本 PR 的最终差异排除，原提交历史仍保留。请不要再按旧本地 head force push 覆盖；先 fetch 后核对新增同步／修复提交。没有必须由你亲自重做的缺口，正常 PR review 后按顺序合并即可。

该 PR 的 GitHub 作者为 `powellzacherymt0rxzw-source`，本轮没有把它擅自对应到某位团队成员姓名。

## 给 ljd（05 → 06）——可在对应 PR 合并后发，不阻塞当前合并

> 现有准备／存储代码已按功能 PR 收口。你开始 05/06 接线前请先读：#82 的 PDF worker 取消与超时回收；#62 的一道大题唯一 q_id、内部小问 metadata；#64 的 BYOK/RPM 并发；#65 的已有 durable handler、artifact 和不确定请求不自动重提；#66 的识别失败原件保留／成功原子替换；#67 的 retained/task_only 知识库引用。不要再按旧附件生成独立 leaf，也不要复制第二套 OCR、队列或恢复机制。你不必现在审所有 PR，最晚在改对应接口前读。

具体影响：05-A §4.7／§5 的 `_pdf_worker` 扩展不能去掉 #82 的可终止进程；06-A/06-B 使用 #62–#65 当前大题合同；06-C/06-D 的失败保留、逐 source 重试和知识库三入口复用 #66/#67。#67 是存储，不是 06-D 共享识别已经完成。

## 给 dsy（07 → 09）——可在对应 PR 合并后发

> 07/09 开始对应接线前请看 #62、#66、#60、#83。当前暂行设计是一道大题一个 q_id，`1.1(a)`、`1.1(b)` 的不同答案块可以归入同一个大题，但必须保留小问标签和来源，不能互相覆盖，也不能为每块复制大题满分。源码题继续按未来冻结的教师函数合同，不按 first-def／文件顺序猜。#66 只有新文件保存并原子采用后才清旧文件；识别失败仍保留可重试原件。#60 保留复核/失败状态语义。#83 现在禁止 production 旧宿主执行，不要把拒绝执行解释成学生测试 0/N。你可在各项合并后读，不必提前审整条栈。

具体影响：07-D1 §5 复用 #82 文件工具；07-D2 §6.2 的归位／一对一检查需适配“不同小问块→同一大题”而不丢块，不能照旧 leaf 示例另建身份；§6.3/§6.4 的 runner canonical Schema 仍由 08 所有者提供；09 §4 的未执行≠0 分和 §7 的正式接线边界保持不变。本次没有替你完成 07/09。

## 给 xts（08）——可在 #83 合并后发

> #83 是一个独立的生产安全保护：正式 OCI 尚未接线前，旧 run_python_subprocess 在 production 明确拒绝执行，避免学生代码运行在网站宿主。它没有实现你负责的 canonical RunnerRequest/Result、harness 或正式 adapter。你做 08、dsy 做 09 时请保留这个拒绝路径；只有经过正式隔离的 adapter 接通后才由对应业务使用，不能临时关闭保护。现有 runner spike 的绿色检查不等于正式 08/09 完成。你最晚在正式接线前读 #83，不需要现在为认证／题目 PR 会签。

## 给存储 reviewer（#66/#67）——合并前

> 请集中核对 #66/#67 的清理授权、失败原件保留、旧版本不能删除新源、删除失败仍占用存储配额并重试，以及 retained/task_only 引用。现在允许批改结束或最终确认触发清理，但必须先证明结构化输入足以恢复；OCR 失败需要原件时不清。新上传成功保存并原子替换后旧件可清；上传尝试失败不能删旧件。两个 PR 都做了真实 PostgreSQL 与迁移往返，同一位 reviewer 可以顺序审核，不需要所有后续开发者参加。

## 给 gxr（11）——页面稳定后发

> BYOK 一页指南仍按任务 11，不需要为本次所有代码 PR 做审核。定稿前从实际候选页面核对字段、三阶段模型能力和错误文案；不要依据旧 #79 或旧 Demo 截图写“已支持正式编程执行／全部 OCR 接线”。有问题反馈具体 PR、候选 SHA 和去标识复现。

## 给 12/13 负责人——部署／管理员实现前

> 从合入后的 main 读取 #61 的认证失效语义、#66/#67 的持久存储与迁移链（末端 0016），以及 #83 的生产执行拒绝。13 停用/恢复应复用并加强现有不可复活的 session 失效机制；12 不重建文件配额／清理。真实 SMTP、数据库/S3 恢复、公开 Admin/Student/Invite 隔离及学校邀请门禁仍须单独验收，合代码不能当作上线 Go。

## 给 #58 原作者（仅需解释，不要求他阻塞其他 PR）

> 我此前错误地批量关闭了原 PR。现在 GitHub 对 #58 返回不能重新打开，且 fork 不允许维护者修改。必要的 PDF 取消/超时安全工作已单独放到 #82，不合入线程解析退步；原 PR 评论和提交保留作历史。没有修改你的 fork。Windows 特定环境问题仍需该环境真实复现，不能把 macOS/Linux 测试写成 Windows 已验收。

## 合并后给全组的一句话（确认事实后再发送）

> 本次已按功能逐个合入 PR，未使用聚合 #79；请先更新 main，不要重复 cherry-pick 旧 PR。题目采用大题唯一 q_id、并发复用 BYOK/RPM、识别失败保留原件，详见逐功能交接文档；05–13 各自范围和负责人未改变。具体已合 PR 与 merge SHA 请以最终合并记录为准。
