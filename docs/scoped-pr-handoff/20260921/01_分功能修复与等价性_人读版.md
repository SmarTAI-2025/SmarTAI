# 分功能 PR 恢复、修复归属与等价性（2026-09-21）

## 本次更正

此前把多项功能合成 #79 并批量关闭原 PR，是交付组织方式错误。本次恢复按功能审阅、按 PR 合并；不把业务方案改回旧附件，也不重做已有功能。#79 已关闭且从未合并，保留其分支和全部评论作为历史与代码比对基准。

#40、#59、#60–#67 已重新打开。#58 实际重新打开失败：GitHub 返回 HTTP 422 `The pull request cannot be reopened`，其 fork 也不允许维护者修改，因此另建单独的 PDF 安全 PR #82。不能把这个例外说成“11 个全部恢复成功”。

#40 与 #59 暂保留 open 便于核对历史，但不是合并链：#40 的旧模拟注册已被 #61 真实页面覆盖；#59 原作者提交已由 #61 承接，旧迁移编号不能再单独合入。它们不是被 #79 取代。可在 #61 合并并核对后分别关闭，不重复合并功能。

## 修复放回哪里

| PR | 保留的原功能／独立新增范围 | 本次最小修改 |
|---|---|---|
| #80，新 | 后端依赖安全 | 4 个文件：安全版本下限、依赖约束、Google SDK 移除参数的兼容及构造测试 |
| #81，新 | 前端依赖安全 | 仅 package.json、package-lock.json；安全更新与干净安装 |
| #82，新 | PDF 子进程安全 | 3 个文件：取消、超时及启动竞态后先回收进程再释放容量；不采用 #58 的线程退步 |
| #83，新 | 生产宿主执行保护 | 4 个文件：生产环境拒绝旧宿主执行、稳定错误、测试；不是正式 OCI 接线 |
| #60，原 | 批改进度、复核状态和已接受置信度策略 | reporter 只由所属 worker 删除，重复领取失败不误删；安全错误日志；排除原 PR 无关项目文档删改 |
| #61，原 | 邮件注册、找回密码和相应前端 | 中性响应之后才查询账号／投递；新增回归；没有新建持久邮件队列 |
| #62，原 | 大题结构后端 | 数学括号、扣分解析、旧 metadata、首小问材料标签；空题干去重崩溃和不同明确大题误合并 |
| #63，原 | rubric 前端编辑 | 扣分解析与当前 main import 冲突；保留 Ask 和原编辑逻辑 |
| #64，原 | 答案并发和进度 | 没有新增业务改造；同步修好后的父层，保留 BYOK 并发／RPM |
| #65，原 | 持久恢复 | 没有新增业务改造；同步父层，保留原 worker／artifact／lease |
| #66，原 | 原件配额、清理与任务删除 | 清理前和实际删除前检查可恢复性；识别失败保留，成功原子替换可清旧件；补回归并保留取消／reporter 双方逻辑 |
| #67，原 | 知识库 retained／task_only 存储 | 没有新增业务改造；同步父层并保留双方 PostgreSQL 测试 |

本轮修改的 8 条原功能分支都用新增提交向前推进，未 force push，也没有删除原作者提交。这里的“没有新增业务改造”不等于没有新同步提交。各 PR 的 Files changed 以其已修前序层为 base，显示自己的功能差异，不是再次把整个项目塞到一项里。

## 怎样证明拆分没有遗漏

参考版本为 #79 的 `3e4426d687e6b0d5e079068ca684411ec83b997d`。拆分业务栈末端为 #67 的 `f93efbcbd888b82a8079e24d9bfc842eb2455b70`。

从未修改的 main `edfdabceca369ccba3f1d62178908303b8f8dec4` 开始，已在本地按 12 个 PR 的顺序逐次实际创建模拟 merge commit。每一步均无冲突，且合并结果的 Git tree 与对应层 HEAD 一致。这些模拟提交只在本地验证，不会另开聚合 PR。

组合与 #79 比较：运行时代码、依赖、CI、配置和工具字节一致。原来混合在一个文件里的 6 个测试函数，按题目／进度拆入两个测试文件；函数体和参数化装饰器 AST 完全一致。不同的是撤回的“只合 #79”交付文档、入口指引，以及测试文件归档；这些不是业务能力遗漏。新的逐功能文档替代错误的操作指引，旧稿仍在关闭的 #79 历史中可读。

## 本地验证结果

| 范围 | 实际结果 |
|---|---|
| 逐层模拟合并 | 12 层全部无冲突；每层合并 tree 与 HEAD 一致 |
| 最终模拟组合完整后端 | 1480 passed、30 skipped、0 failed |
| 额外安全边界回归 | 93 passed；认证、原件、PDF、生产执行、网络政策、reporter |
| 涉及前端的 8 个层 | 各自完整单测、typecheck、scope、build 全部通过 |
| 真实 PostgreSQL | #61：9 passed；#66：17 passed；#67：23 passed |
| 对应迁移往返 | #61／#66／#67 各自 upgrade → downgrade 0012 → upgrade 通过 |
| 最终组合浏览器 E2E | 2 passed，合成账号和 fake provider |

初次 PostgreSQL 尝试因新建测试库编码及测试 URL 配置失败，修正的是工作区内测试环境，不是应用代码；最终结果以上表和带 `final` 的日志为准。测试集合有重叠，不能相加；跳过不是通过。

远端 CI 在每个 exact head 上单独记录。现有 CI 只对 main-base PR 自动触发，其他堆叠层使用仓库已有 workflow_dispatch，不为测试便利修改 CI。最终状态以 PR 最新验证评论及交付清单为准，不能复用 #79 的绿色图标冒充这些 PR 已通过。

## 独立审核能力的真实边界

本轮没有可调用、且在授权目录内已登录的独立 subagent 执行入口，因此没有完成“3 个独立 subagent 审核”。没有把三个测试进程或同一助手三遍核对写成三个独立 AI。已做的是本助手代码核对、确定性等价／合并检查、额外安全回归及浏览器／数据库／CI 验证。任何需要独立人工审核的关键范围仍须由合格审阅者核对。

没有代为 APPROVE 或合并任何 PR，也没有修改仓库保护规则。真实邮件、模型、AWS/S3、Windows、正式 OCI 业务接线与学校上线许可仍是独立验收；这些本地测试不承诺绝无未知漏洞。

## 本次续接最终复核

重新从 main 实际逐层创建 12 次本地模拟 merge；每层 tree 与对应 PR HEAD 一致，运行时代码与 #79 字节一致，6 个移动测试函数/装饰器 AST 一致。再次完整后端 1480 passed / 30 skipped；前端 378 passed、typecheck/scope/build；安全专项 179 passed。12 个功能 PR 的独立 exact-head workflow_dispatch CI 全部 success。新增验证见 verification-final.json 与 ci-exact-heads.json；本次没有新增业务代码。独立 AI subagent 执行数仍为 0，不把上述三个检查方向写成三个代理。
